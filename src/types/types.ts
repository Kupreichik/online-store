export type routerMode = 'history' | 'hash';

export type callbackType<T> = (arg?: T | undefined) => void;

export type regArray = {
  0?: string;
  groups: undefined;
  index: 0;
  input: 'id/15';
  length: 1;
};
